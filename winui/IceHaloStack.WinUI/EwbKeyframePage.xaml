<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="IceHaloStack_WinUI.EwbKeyframePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:IceHaloStack_WinUI.Controls">
    <Grid Padding="8" RowSpacing="7">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <Button Content="← 返回节点工作流" Click="Back_Click" Margin="0,0,8,0"/>
            <TextBlock Grid.Column="1" Text="曝光 / 白平衡关键帧工作区" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="5">
                <Button Content="分析全部帧" Command="{Binding AnalyzeCommand}" Style="{StaticResource AccentButtonStyle}"/>
                <Button Content="取消" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </Grid>
        <InfoBar Grid.Row="1" IsOpen="{Binding HasError}" Severity="Error" IsClosable="False" Message="{Binding Error}"/>
        <Grid Grid.Row="2" ColumnSpacing="7">
            <Grid.ColumnDefinitions><ColumnDefinition Width="300" MinWidth="250"/><ColumnDefinition Width="*" MinWidth="420"/><ColumnDefinition Width="330" MinWidth="290"/></Grid.ColumnDefinitions>
            <controls:EwbFrameTable/>
            <controls:EwbPreviewPane Grid.Column="1"/>
            <controls:EwbAdjustmentPane Grid.Column="2"/>
        </Grid>
        <StackPanel Grid.Row="3" Spacing="3">
            <ProgressBar Maximum="100" Value="{Binding ProgressPercent}" IsIndeterminate="{Binding IsBusy}"/>
            <TextBlock Text="{Binding Status}" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Page>
